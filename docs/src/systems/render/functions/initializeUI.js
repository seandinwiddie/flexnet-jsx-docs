// === Initialize UI ===
// Simple UI initialization for compatibility

export const initializeUI = () => {
    // Simple UI initialization
    console.log('UI initialized');
    return Promise.resolve();
}; 